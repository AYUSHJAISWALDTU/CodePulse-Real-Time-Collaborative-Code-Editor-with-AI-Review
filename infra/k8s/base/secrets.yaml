# ⚠ This is a placeholder. In production, use a Secret Manager
# or sealed-secrets, NEVER commit real secrets to git.
apiVersion: v1
kind: Secret
metadata:
  name: codepulse-secrets
  namespace: codepulse
type: Opaque
stringData:
  DATABASE_URL: "postgresql://codepulse:CHANGE_ME@postgres:5432/codepulse"
  REDIS_URL: "redis://redis:6379"
  JWT_SECRET: "CHANGE_ME_TO_A_RANDOM_64_CHAR_STRING"
  OPENAI_API_KEY: "sk-CHANGE_ME"
